"""
Load command.

Handles:

    structtool load
"""


def load_structure(
    struct_name,
    destination
):
    """
    Recreate a structure
    into destination folder.

    Uses latest version.
    """